**Proxy may not work**
The proxy uses an API to make the proxy work. It is not as good as Scramjet and Ultraviolet. **EXPECT SOME BUGS** The proxy only contains one file to make it work, which is index.html itself.
